gerrit:
  servers:
    - name: "gerrit-test-server"
      noConnectionOnStartup: true
      config:
        gerritAuthKeyFile: "/var/jenkins_home/gerrit_key.pem</gerritAuthKeyFile"
        gerritUserName: "admin"